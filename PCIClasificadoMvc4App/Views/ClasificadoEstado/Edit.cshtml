@model PCIClasificadoMvc4App.Models.ClasificadoEstado

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ClasificadoEstado</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.estadoId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.estadoId)
            @Html.ValidationMessageFor(model => model.estadoId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.estadoNombre)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.estadoNombre)
            @Html.ValidationMessageFor(model => model.estadoNombre)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
